import {FC} from 'react';

type AboutPagePT = {
  // add props type
};

const AboutPage: FC<AboutPagePT> = ({}) => {
  return (
      <div>
        AboutPage
      </div>
  );
};

export default AboutPage